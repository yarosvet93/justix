# Project instructions

Do not read, print, modify, or commit `.env`.
Do not expose Telegram tokens, chat IDs, proxy credentials, private keys, or secrets.
Use `.env.example` for environment variable examples.
Public web files must stay under `public/`.
